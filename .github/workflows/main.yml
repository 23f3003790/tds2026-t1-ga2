name: 23f3003790@ds.study.iitm.ac.in

on:
  push:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - name: 23f3003790@ds.study.iitm.ac.in
        run: echo "Hello, world!"
